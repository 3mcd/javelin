# `@javelin/example-networking`

An example of a synchronized ECS over WebRTC data channels (unreliable and reliable). Uses network protocol and utilities from `@javelin/net`.

<img src="./screenshot.png">
